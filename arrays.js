// arrays are the collections in javascript
// they can have multiple types of values in them
let human = {
    name: "vageta",
    age: "unknown"
};
let myCollection = ["moon", 10, human ]

console.log(myCollection[2].age, myCollection[2].name,)
console.log(myCollection.length)